/**
 * Z-index scale. Values are mirrored as CSS variables in globals.css
 * (--z-menu, --z-nav, --z-viewer, --z-toast, --z-grain).
 */
export const Z = {
  menu: 30,
  nav: 40,
  viewer: 60,
  toast: 70,
  grain: 80,
} as const;
